# Task 6.8 - Domain, SSL & Production Release

> **Trạng thái:** Source/local gate đã hoàn thành; production release đang chờ hạ tầng và phê duyệt
> **Yêu cầu trước:** Task 6.1-6.7 và staging evidence

## Mục tiêu

Phát hành production bằng canonical domain, TLS tự gia hạn, backup trước release,
role smoke, rollback có kiểm soát và theo dõi sau phát hành.

## Phạm vi

- DNS, HTTPS/TLS renewal và reverse proxy edge.
- Secure cookies, CORS, HSTS và canonical origin production.
- Pre-release encrypted backup và migration policy backward-compatible.
- Public/admin/teacher/student smoke không sửa dữ liệu.
- Maintenance window, release ownership, rollback và 24h monitoring.

## Tự kiểm tra

- [x] Production Compose overlay và Caddy config được source-control.
- [x] Caddyfile được snapshot trong persistent release state, không phụ thuộc runner checkout.
- [x] Production env checker chặn HTTP, localhost, cookie không secure và secret yếu.
- [x] Readiness checker xác minh DNS/TLS/redirect/security headers/health/database.
- [x] Business-read-only role smoke kiểm tra admin/teacher/student, forbidden admin API và thu hồi auth session.
- [x] Workflow tạo encrypted backup trước deploy và chỉ auto-rollback sau khi deploy thực sự hoàn tất.
- [x] Local Compose/Caddy config validation pass.
- [x] Production deploy command assembly/snapshot pass ở validate-only mode.
- [ ] DNS thật trỏ đúng edge host và canonical HTTPS pass.
- [ ] GitHub production Environment, reviewer, secrets và smoke accounts được cấu hình.
- [ ] Pre-release backup production được tạo và offsite sync xác minh.
- [ ] Production role smoke và rollback command được thực thi có evidence.
- [ ] 24h post-release review hoàn thành, không còn P0/P1.

## Definition of Done

Không đánh dấu `[x]` trong `WORKFLOW.md` chỉ vì source/build pass. Task chỉ hoàn
thành khi có production evidence cho DNS/TLS, backup, role smoke, rollback và 24h
monitoring. Xem runbook tại `docs/production-release.md`.

## Khu vực report

```text
[2026-08-01]
- Source: Caddy edge overlay, env/readiness/role-smoke checkers và release gates.
- Security: backend/database giữ loopback; HTTP redirect; HSTS không includeSubDomains/preload; production trust proxy = 2 hops.
- Backup: workflow bắt buộc encrypted backup trước production deploy.
- Rollback: chỉ chạy tự động khi DEPLOY_COMPLETED=true; không rollback database.
- Local evidence: env self-test pass (1 valid/5 invalid); Compose+Caddy/deploy validate-only pass; local readiness pass; admin/teacher/student role smoke và forbidden users API pass; db:setup/backend quality pass trên PostgreSQL 18 cô lập; frontend build pass.
- Local drift: `.env.docker` hiện không mở được connection mới tới demo volume (28P01); runtime pool vẫn healthy. Không đổi password trong task này.
- External gate: DNS, production secrets/accounts, release execution và 24h review.
```
