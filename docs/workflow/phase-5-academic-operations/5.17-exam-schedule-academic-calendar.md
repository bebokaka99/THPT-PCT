# Task 5.17 - Exam Schedule & Academic Calendar

> **Phase:** 5
> **Ưu tiên:** P1
> **Trạng thái:** Hoàn thành
> **Yêu cầu trước:** 5.1, 5.2, 5.3, 5.4, 5.15

## Mục tiêu

Quản lý lịch kiểm tra/thi, học bù, nghỉ học và sự kiện học vụ theo lớp/môn,
tách khỏi module sự kiện công khai.

## Phạm vi bắt buộc

- Lịch học vụ có loại: kiểm tra, thi, học bù, nghỉ học, hạn học vụ.
- Kiểm tra trùng lớp, giáo viên và phòng với timetable/override hiện hành.
- Admin tạo/công bố; giáo viên chỉ tạo đề xuất trong phạm vi assignment nếu
  policy cho phép.
- Student/guardian xem lịch theo enrollment; teacher xem lịch cá nhân.
- Notification khi lịch published hoặc thay đổi.
- Calendar/list view, filter và export/print cơ bản.

## Ngoài phạm vi

- Thi trực tuyến và ngân hàng câu hỏi.
- Thuật toán tự xếp lịch thi toàn trường.

## Tự kiểm tra bắt buộc

- [x] Student không xem lịch lớp khác
- [x] Teacher không tạo lịch cho môn/lớp ngoài assignment
- [x] Draft không tạo notification
- [x] Publish/update tạo notification phù hợp
- [x] Conflict phòng/lớp/giáo viên bị chặn
- [x] Timezone Asia/Ho_Chi_Minh được kiểm tra

## Definition of Done

Mỗi vai trò nhìn thấy đúng lịch học vụ đã công bố và thay đổi lịch có kiểm soát,
notification và lịch sử.

## Report

### 2026-08-01

- Database: thêm `academic_calendar_entries` và audit bất biến, tham chiếu năm
  học, học kỳ, classroom, subject, teaching assignment và teacher. Migration
  `038` chỉ cho phép cascade audit trong transaction xóa draft/proposal hợp lệ;
  migration `039` thêm DB guard chặn publish khi có conflict.
- Workflow: admin tạo draft, publish/archive; teacher chỉ tạo/sửa proposal từ
  assignment active của chính mình; student và guardian chỉ đọc lịch published
  theo enrollment/link đã xác minh.
- Conflict: khi preview/publish/update lịch published, backend kiểm tra overlap
  lớp, giáo viên, phòng với lịch học vụ published và thời khóa biểu published.
  Test/thi trùng đúng tiết của cùng assignment được xem là lịch thay thế tiết;
  học bù vẫn bị kiểm tra xung đột đầy đủ.
- Notifications: draft/proposal không gửi; publish và cập nhật lịch published
  gửi riêng cho student, guardian và teacher phù hợp.
- UI: thêm `/admin/academic-calendar`, `/teacher/academic-calendar`,
  `/student/academic-calendar`, `/parent/academic-calendar`; có list/calendar,
  filter, conflict preview, print và export ICS.
- Validation: timestamp API bắt buộc timezone; frontend gửi `+07:00`; backend
  truy vấn enrollment và timetable theo `Asia/Ho_Chi_Minh`.
- Quality: migration/seed trên PostgreSQL Docker, backend build, frontend build
  và smoke test scope/conflict/notification/audit đều pass.
- Giới hạn: lịch dạy thay/đổi tiết theo ngày chưa được dùng làm conflict source;
  phần này thuộc Task 5.18.
- Next task: 5.18 - Substitute Teaching & Daily Schedule Overrides.
