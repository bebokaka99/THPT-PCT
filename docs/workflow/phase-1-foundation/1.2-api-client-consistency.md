# Task 1.2 — API Client Consistency

> **Phase:** 1 — Foundation
> **Trạng thái:** ✅ Hoàn thành
> **Độ khó:** Thấp
> **Thời gian ước tính:** 30 phút
> **Risk nếu không làm:** 🟢 LOW — Inconsistency nhỏ nhưng tạo confusion
> **Yêu cầu trước:** Không

---

## Mô tả vấn đề

`frontend/src/services/adminMedia.service.ts` dùng **raw `fetch`** tự xây `buildUrl`, `parseResponse`, `authHeaders` riêng — trong khi tất cả service files khác dùng `apiClient` từ `api-client.ts`.

---

## Mục tiêu

1. Refactor `adminMedia.service.ts` dùng `apiClient`
2. Xử lý đặc biệt: upload media dùng `FormData` (không JSON) → cần mở rộng `apiClient`

---

## Cách thực hiện

### Plan A — Thêm method `upload` vào `apiClient` (KHUYẾN NGHỊ)

**Bước 1:** Thêm method `upload` vào `frontend/src/services/api-client.ts`

```ts
upload<T>(path: string, formData: FormData, options?: RequestOptions) {
  return request<T>(
    path,
    {
      method: 'POST',
      body: formData,
      headers: {}, // KHÔNG set Content-Type — browser tự set boundary
    },
    options,
  );
},
```

Lưu ý: Phải sửa hàm `request` để KHÔNG set `Content-Type: application/json` khi body là FormData.

```ts
async function request<T>(path: string, init: RequestInit = {}, options: RequestOptions = {}) {
  const isFormData = init.body instanceof FormData;
  const headers: Record<string, string> = {
    ...(isFormData ? {} : { 'Content-Type': 'application/json' }),
    ...Object.fromEntries(new Headers(options.headers).entries()),
    ...Object.fromEntries(new Headers(init.headers).entries()),
  };
  // ... rest giữ nguyên
}
```

**Bước 2:** Refactor `adminMedia.service.ts`

```ts
import { apiClient } from './api-client';
import type { ApiDataResponse, ApiPaginatedResponse } from '../types/api';
import type { MediaFile, MediaListQuery } from '../types/media';

function authHeaders(token: string) {
  return { Authorization: `Bearer ${token}` };
}

export function uploadMedia(token: string, file: File) {
  const formData = new FormData();
  formData.append('file', file);
  return apiClient.upload<ApiDataResponse<MediaFile>>('/media/upload', formData, {
    headers: authHeaders(token),
  });
}

export function getAdminMedia(token: string, query: MediaListQuery = {}) {
  return apiClient.get<ApiPaginatedResponse<MediaFile>>('/media', {
    headers: authHeaders(token),
    params: { type: query.type ?? 'all', page: query.page ?? 1, limit: query.limit ?? 20 },
  });
}

export function deleteAdminMedia(token: string, id: number) {
  return apiClient.delete<void>(`/media/${id}`, { headers: authHeaders(token) });
}
```

**Bước 3:** Xóa hàm `buildUrl`, `parseResponse`, `apiBaseUrl` local trong `adminMedia.service.ts`

**Bước 4:** Verify
- Upload 1 file image qua admin media page
- List media files
- Delete 1 file

### Plan B — Nếu FormData request bị lỗi Content-Type

**Lý do:** `apiClient` override Content-Type header → FormData boundary bị mất.

**Cách xử lý:** Đảm bảo khi body là `FormData`, headers KHÔNG chứa `Content-Type`. Browser tự thêm `multipart/form-data; boundary=...`.

### Plan C — Giữ raw fetch cho upload, chỉ refactor get/delete

Nếu Plan A quá phức tạp: chỉ refactor `getAdminMedia` và `deleteAdminMedia` dùng `apiClient`, giữ `uploadMedia` dùng raw fetch.

---

## Checklist thực hiện

- [x] Sửa `api-client.ts` — thêm `upload` method + handle FormData
- [x] Refactor `adminMedia.service.ts` — dùng `apiClient`
- [x] Xóa local `buildUrl`, `parseResponse`, `apiBaseUrl`
- [x] Test upload file
- [x] Test list media
- [x] Test delete media
- [x] Đánh dấu hoàn thành ✅

---

## Khu vực Report

```
[2026-07-25]
- `apiClient` hỗ trợ `upload` bằng `FormData` và tự bỏ `Content-Type` để trình duyệt
  tự thêm multipart boundary.
- `adminMedia.service.ts` đã dùng chung `apiClient` cho upload, list và delete;
  không còn raw `fetch` hoặc helper URL/response riêng.
- Frontend build pass.
- Smoke test với backend đang chạy: upload ảnh, list media và delete record đều pass.
- Kiểm tra source cho thấy raw `fetch` chỉ còn nằm trong `api-client.ts`.
```

---

> Cập nhật lần cuối: 2026-07-25
