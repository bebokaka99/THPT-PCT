# Task 1.1 — Extract Shared Validators

> **Phase:** 1 — Foundation
> **Trạng thái:** ✅ Hoàn thành
> **Độ khó:** Thấp-Trung bình
> **Thời gian ước tính:** 1-2 giờ
> **Risk nếu không làm:** 🟡 Tech debt compound mỗi module mới
> **Yêu cầu trước:** Không

---

## Mô tả vấn đề

Các hàm validation sau bị **copy-paste y hệt** ở 6+ modules:
- `asRecord(body)` — xuất hiện trong: user, post, document, category, classroom, importer, notification, timetable validations
- `optionalString(value, field)` — 6+ files
- `parsePositiveInteger(value, fallback, field)` — 5+ files
- `firstQueryValue(value)` — 5+ files
- `optionalNullableString(value, field)` — 4+ files
- `optionalStatus(value)` pattern — mỗi module có riêng nhưng logic giống nhau

Tổng cộng khoảng **200-300 dòng code duplicate**.

---

## Mục tiêu

1. Tạo `backend/src/validators/common.ts` chứa tất cả shared validators
2. Refactor từng module validation import từ shared
3. Giữ nguyên behavior — KHÔNG thay đổi logic, chỉ move code

---

## Cách thực hiện

### Plan A — Extract rồi refactor từng file (KHUYẾN NGHỊ)

**Bước 1:** Tạo `backend/src/validators/common.ts`

```ts
import { HttpError } from '../utils/http-error.js';

export function asRecord(body: unknown): Record<string, unknown> {
  if (typeof body !== 'object' || body === null) {
    throw new HttpError(400, 'Request body is required');
  }
  return body as Record<string, unknown>;
}

export function firstQueryValue(value: unknown) {
  return Array.isArray(value) ? value[0] : value;
}

export function optionalString(value: unknown, field: string) {
  if (value === undefined || value === null) return undefined;
  if (typeof value !== 'string') throw new HttpError(400, `${field} must be a string`);
  const trimmed = value.trim();
  return trimmed || undefined;
}

export function requiredString(value: unknown, field: string) {
  const parsed = optionalString(value, field);
  if (!parsed) throw new HttpError(400, `${field} is required`);
  return parsed;
}

export function optionalNullableString(value: unknown, field: string) {
  if (value === undefined) return undefined;
  if (value === null) return null;
  if (typeof value !== 'string') throw new HttpError(400, `${field} must be a string`);
  const trimmed = value.trim();
  return trimmed || null;
}

export function parsePositiveInteger(value: unknown, fallback: number, field: string) {
  const rawValue = firstQueryValue(value);
  if (rawValue === undefined || rawValue === null || rawValue === '') return fallback;
  const numberValue = Number(rawValue);
  if (!Number.isInteger(numberValue) || numberValue <= 0) {
    throw new HttpError(400, `${field} must be a positive integer`);
  }
  return numberValue;
}

export function optionalPositiveId(value: unknown, field: string) {
  if (value === undefined || value === null || value === '') return undefined;
  const id = Number(value);
  if (!Number.isInteger(id) || id <= 0) throw new HttpError(400, `${field} must be a positive integer`);
  return id;
}

export function requiredPositiveId(value: string, field = 'id') {
  const id = Number(value);
  if (!Number.isInteger(id) || id <= 0) throw new HttpError(400, `Invalid ${field}`);
  return id;
}

export function optionalNullableId(value: unknown, field: string) {
  if (value === undefined) return undefined;
  if (value === null) return null;
  const id = Number(value);
  if (!Number.isInteger(id) || id <= 0) throw new HttpError(400, `${field} must be a positive integer`);
  return id;
}

export function optionalBoolean(value: unknown, field: string) {
  if (value === undefined || value === null) return undefined;
  if (typeof value !== 'boolean') throw new HttpError(400, `${field} must be a boolean`);
  return value;
}

export function validateEnumValue<T extends string>(
  value: unknown,
  validValues: Set<T>,
  field: string,
  required = false,
): T | undefined {
  if (value === undefined || value === null || value === '') {
    if (required) throw new HttpError(400, `${field} is required`);
    return undefined;
  }
  if (typeof value !== 'string' || !validValues.has(value as T)) {
    throw new HttpError(400, `${field} must be one of: ${[...validValues].join(', ')}`);
  }
  return value as T;
}

export function buildPaginationQuery(
  query: Record<string, unknown>,
  maxLimit = 50,
) {
  return {
    page: parsePositiveInteger(query.page, 1, 'page'),
    limit: Math.min(parsePositiveInteger(query.limit, 10, 'limit'), maxLimit),
  };
}
```

**Bước 2:** Refactor từng file validation (1 file 1 lần, test sau mỗi file)

Thứ tự refactor:
1. `user.validation.ts` — thay `asRecord`, `optionalString`, `parsePositiveInteger`, `firstQueryValue`
2. `post.validation.ts` — thay tương tự
3. `document.validation.ts`
4. `category.validation.ts`
5. `classroom.validation.ts`
6. `importer.validation.ts`
7. `notification.validation.ts`
8. `timetable.validation.ts`

Mỗi file: import từ `../../validators/common.js` → xóa local definitions → test API tương ứng

**Bước 3:** Verify toàn bộ

```bash
cd backend
npx tsc --noEmit  # Type check
npm run dev       # Chạy server
# Test từng endpoint group
```

### Plan B — Nếu refactor gây lỗi type

**Lý do:** Một số module có `optionalString` trả về type hơi khác (ví dụ `string | null` vs `string | undefined`).

**Cách xử lý:**
- Tạo 2 variant: `optionalString` (trả `undefined`) và `optionalNullableString` (trả `null`)
- Kiểm tra return type từng module trước khi thay

### Plan C — Nếu quá nhiều file sửa 1 lúc

**Cách xử lý:** Refactor từng module 1, mỗi module commit riêng:
1. Tuần 1: user + post validators
2. Tuần 2: document + category + classroom validators
3. Tuần 3: importer + notification + timetable validators

---

## Checklist thực hiện

- [x] Tạo `backend/src/validators/common.ts`
- [x] Refactor `user.validation.ts` → import shared → xóa local copies
- [x] Test user API endpoints
- [x] Refactor `post.validation.ts` → import shared → xóa local copies
- [x] Test post API endpoints
- [x] Refactor `document.validation.ts` → import shared → xóa local copies
- [x] Test document API endpoints
- [x] Refactor `category.validation.ts` → import shared → xóa local copies
- [x] Test category API endpoints
- [x] Refactor `classroom.validation.ts` → import shared → xóa local copies
- [x] Test classroom API endpoints
- [x] Refactor `importer.validation.ts` → import shared → xóa local copies
- [x] Refactor `notification.validation.ts` → import shared → xóa local copies
- [x] Refactor `timetable.validation.ts` → import shared → xóa local copies
- [x] Chạy `npx tsc --noEmit` → 0 errors
- [x] Đánh dấu hoàn thành ✅

---

## Khu vực Report

```text
[2026-07-25]
- Thêm backend/src/validators/common.ts.
- Refactor shared helpers trên auth, users, posts, documents, categories,
  classrooms, importer, media, notifications, profiles và timetables.
- Giữ riêng các rule domain-specific như enum status, MIME type, slug và post image.
- Common validators unit test: pass.
- Backend quality gate: pass.
- Frontend build: pass.
```

---

> Cập nhật lần cuối: 2026-07-25
