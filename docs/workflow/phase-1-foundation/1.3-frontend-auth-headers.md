# Task 1.3 — Frontend Auth Headers Utility

> **Phase:** 1 — Foundation
> **Trạng thái:** ✅ Hoàn thành
> **Độ khó:** Thấp
> **Thời gian ước tính:** 30 phút
> **Risk nếu không làm:** 🟢 LOW — Code smell, không phải bug
> **Yêu cầu trước:** Task 1.2

---

## Mô tả vấn đề

Hàm `authHeaders(token)` bị copy-paste ở **7 service files** frontend:
- `adminCategory.service.ts`
- `adminDocument.service.ts`
- `adminImporter.service.ts`
- `adminMedia.service.ts`
- `adminPost.service.ts`
- `adminRole.service.ts`
- `adminUser.service.ts`

---

## Mục tiêu

1. Tạo utility `authHeaders` dùng chung
2. Refactor tất cả service files import từ utility

---

## Cách thực hiện

### Plan A — Extract vào `api-client.ts` (KHUYẾN NGHỊ)

**Bước 1:** Thêm vào `frontend/src/services/api-client.ts`:

```ts
export function authHeaders(token: string) {
  return { Authorization: `Bearer ${token}` };
}
```

**Bước 2:** Tìm và thay thế ở tất cả 7+ files:
- Xóa local `function authHeaders`
- Thêm `import { authHeaders } from './api-client';`

**Bước 3:** Verify bằng TypeScript check: `npx tsc --noEmit`

### Plan B — Nếu circular dependency

Nếu import từ `api-client` gây circular → tạo file riêng `frontend/src/services/auth-headers.ts`

---

## Checklist thực hiện

- [x] Thêm `authHeaders` vào `api-client.ts`
- [x] Refactor 7 service files → import shared
- [x] Xóa 7 local `authHeaders` functions
- [x] `npx tsc --noEmit` → 0 errors
- [x] Test 1 admin page (posts) → vẫn load data OK
- [x] Đánh dấu hoàn thành ✅

---

## Khu vực Report

```
[2026-07-25]
- Thêm `authHeaders(token)` dùng chung trong `api-client.ts`.
- Refactor toàn bộ 11 service có Bearer token: 7 admin service cùng auth,
  classroom, notification và profile service.
- Không còn local `authHeaders` hoặc Bearer header viết trực tiếp ngoài
  `api-client.ts`.
- `npm run build` frontend pass, bao gồm TypeScript project build.
- Login admin và request danh sách admin posts bằng Bearer token pass trên
  backend local.
```

---

> Cập nhật lần cuối: 2026-07-25
