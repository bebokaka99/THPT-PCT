# Task 1.5 — Backend Logging Setup

> **Phase:** 1 — Foundation
> **Trạng thái:** ✅ Hoàn thành
> **Độ khó:** Thấp
> **Thời gian ước tính:** 1 giờ
> **Risk nếu không làm:** 🟡 MEDIUM — Production không có log → blind debugging
> **Yêu cầu trước:** Không

---

## Mô tả vấn đề

Backend hiện dùng `console.log` / `console.warn` / `console.error` rải rác. Không có structured logging, không có request ID, không phân biệt log level.

---

## Mục tiêu

1. Cài đặt structured logger (pino hoặc winston)
2. Request logging middleware (method, path, status, duration)
3. Thay thế `console.*` calls trong codebase

---

## Cách thực hiện

### Plan A — Pino (nhẹ, nhanh, KHUYẾN NGHỊ)

**Bước 1:** Cài pino
```bash
cd backend
npm install pino pino-pretty
npm install -D @types/pino 2>/dev/null || true
```

**Bước 2:** Tạo `backend/src/utils/logger.ts`
```ts
import pino from 'pino';

export const logger = pino({
  level: process.env.LOG_LEVEL ?? 'info',
  transport: process.env.NODE_ENV !== 'production'
    ? { target: 'pino-pretty', options: { colorize: true } }
    : undefined,
});
```

**Bước 3:** Tạo request logging middleware `backend/src/middlewares/request-logger.ts`
```ts
import type { RequestHandler } from 'express';
import { logger } from '../utils/logger.js';

export const requestLogger: RequestHandler = (req, res, next) => {
  const start = Date.now();
  res.on('finish', () => {
    logger.info({
      method: req.method,
      path: req.path,
      status: res.statusCode,
      duration: Date.now() - start,
    }, `${req.method} ${req.path} ${res.statusCode}`);
  });
  next();
};
```

**Bước 4:** Mount trong `app.ts` TRƯỚC routes
```ts
app.use(requestLogger);
```

**Bước 5:** Thay `console.*` → `logger.*` trong:
- `server.ts`: `console.log` → `logger.info`
- `error.middleware.ts`: `console.warn` → `logger.warn`
- `migrate.ts`: `console.log/error` → `logger.info/error`

### Plan B — Winston (nếu cần file logging)

Nếu cần ghi log ra file:
```bash
npm install winston
```
Config tương tự nhưng thêm file transport.

### Plan C — Chỉ tạo wrapper, không cài thư viện

```ts
export const logger = {
  info: (...args: unknown[]) => console.log('[INFO]', new Date().toISOString(), ...args),
  warn: (...args: unknown[]) => console.warn('[WARN]', new Date().toISOString(), ...args),
  error: (...args: unknown[]) => console.error('[ERROR]', new Date().toISOString(), ...args),
};
```

---

## Checklist thực hiện

- [x] Cài `pino` + `pino-pretty`
- [x] Tạo `backend/src/utils/logger.ts`
- [x] Tạo `backend/src/middlewares/request-logger.ts`
- [x] Mount `requestLogger` trong `app.ts`
- [x] Thay `console.*` → `logger.*` (server, error middleware, migrate)
- [x] Chạy `npm run dev` → verify log output đẹp
- [x] Đánh dấu hoàn thành ✅

---

## Khu vực Report

```
[2026-07-25]
- Cài `pino` và `pino-pretty`.
- Thêm logger structured với level theo `LOG_LEVEL`; development dùng pretty output,
  production dùng JSON output phù hợp log collector.
- Thêm request logger ghi requestId, method, path, statusCode và durationMs.
- Chuẩn hóa log của server, error middleware, environment warning và migration runner.
- `npm run build` backend pass.
- `npm run db:status` pass và hiển thị structured migration logs.
- Endpoint `/api/health` đang chạy bình thường trên backend local.
```

---

> Cập nhật lần cuối: 2026-07-25
